package operators;

public class Shift_operator {
    public static void main(String[] args) {
        int RS =25;
        int LS=1;
        System.out.println(RS>>LS);   // formula for right shift (a/2^b)
        System.out.println(RS<<LS);   // formula for left shift (a*2^b)
    }
}
